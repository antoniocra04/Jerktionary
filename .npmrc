# Peer deps must be installed and recorded in package-lock.json: CI runs
# plain `npm ci` with default config, and a lockfile generated under
# legacy-peer-deps=true (e.g. from a user-level ~/.npmrc) fails there with
# "Missing: <pkg> from lock file".
legacy-peer-deps=false
